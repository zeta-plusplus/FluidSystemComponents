within FluidSystemComponents.Compressible.Components;

model bdry
equation

annotation(
    Icon(graphics = {Line(points = {{-200, 0}, {200, 0}}, pattern = LinePattern.Dash, thickness = 4)}));
end bdry;
